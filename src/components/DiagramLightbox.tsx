/**
 * DiagramLightbox — placeholder component for workflow diagrams.
 * Will be replaced with a full lightbox viewer when diagram assets are ready.
 */
const DiagramLightbox = () => {
  return (
    <div className="my-8 rounded-lg border-2 border-dashed border-muted-foreground/25 bg-muted/30 p-8 text-center">
      <p className="text-sm text-muted-foreground italic">
        [ Workflow diagram placeholder — diagrams will be added here ]
      </p>
    </div>
  );
};

export default DiagramLightbox;
